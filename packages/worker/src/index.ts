// HTMLess Worker — Webhook delivery and async jobs
// This will be expanded in Phase 5 (Integrations & Extensibility)
// For now, it's a placeholder that connects to Redis

console.log('HTMLess Worker starting...');
console.log('Worker ready (webhook delivery will be added in Phase 5)');

// Keep process alive
setInterval(() => {}, 60_000);
